# Registry is inherited from ~/.npmrc (Databricks internal proxy).
# openai declares an optional peer on zod@3 while @openai/agents requires zod@4 —
# an unsatisfiable optional-peer split, so accept the resolved tree.
legacy-peer-deps=true
